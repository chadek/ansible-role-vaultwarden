__vaultwarden_build_packages:
  - gcc
  - openssl-devel
  - pkg-config
  - tar
  - curl

__vaultwarden_backend_build_packages:
  {
    sqlite: [sqlite-devel],
    postgresql: [libpq-devel],
    mysql: [mariadb-devel, mysql-libs],
  }
